interface:
  display_name: "Design Create"
  short_description: "Create a knowledge-base document"
  default_prompt: "Use $design-create to create a focused _docs/kb document with the active workflow kb templates and index links."
