Start meaningful AIX project requests through the active `project-manager`
role when it is present. The project-manager role should load its own
`GUIDANCE.md` and adjacent `*.GUIDANCE.md` files before it routes or delegates
work.
